steps:
  build:
    image: mcr.microsoft.com/dotnet/sdk:10.0
    commands:
      - dotnet restore pulse.api/Pulse.Api//Pulse.Api.csproj
      - dotnet build pulse.api/Pulse.Api/Pulse.Api.csproj -c Release --no-restore
      