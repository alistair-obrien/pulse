{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*",

    "ConnectionStrings": {
        // Default PW is saved in dotnet secrets
        "Pulse": "Host=localhost;Port=5432;Database=pulse"
    }

    //// Actually will not be here but rather local to users device as the API only gives your own data
    //"Hevy": {
    //    // API Key from dotnet user secrets
    //    "BaseUrl": "https://api.hevyapp.com/v1/",
    //    "Timeout": 30
    //}
}
