{
  "name": "pulse-web-ts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    ">>> SERVER <<<": "Builds and runs the server on local machine",
    "server:development": "dotnet --version TODO: Run the server specifically on host and port",
    ">>> TEST <<<": "TODO: Run tests",
    ">>> DEPLOY <<<": "TODO: Deploy to app stores and web server",
    ">>> PACKAGE NATIVE <<<": "",
    ">>> BUILD NATIVE <<<": "Builds natively. Requires signing",
    "build_native:android": "npx cap sync android && npx cap build android",
    "build_native:ios": "npx cap sync ios && npx cap build ios",
    ">>> RUN <<<": "Runs the app on the device",
    "run:android": "npx cap run android",
    "run:ios": "npx cap run ios",
    ">>> BUILD AND RUN <<<": "Builds and runs the app",
    "< Build Development >": "",
    "build:development:web": "vite build --mode development",
    "build:development:android": "npm run build:development:web && npm run run:android",
    "build:development:ios": "npm run build:development:web && npm run run:ios",
    "build:development": "npm run build:development:web && npm run run:android && npm run run:ios",
    "< Build Staging >": "",
    "build:staging:web": "tsc && vite build --mode staging",
    "build:staging:android": "npm run build:staging:web && npm run run:android",
    "build:staging:ios": "npm run build:staging:web && npm run run:ios",
    "build:staging": "npm run build:staging:web && npm run run:android && npm run run:ios",
    "< Build Production >": "TODO: Make sure the native builds are made and deployed",
    "build:production:web": "tsc && vite build --mode production",
    "build:production:android": "npm run build:production:web && npm run run:android",
    "build:production:ios": "npm run build:production:web && npm run run:ios",
    "build:production": "npm run build:production:web && npm run run:android && npm run run:ios",
    ">>> VITE LIVE RELOAD <<<": "Starts Vite live reload server",
    "< Live Reload Development >": "",
    "live:development:web": "vite --host --mode development --open",
    "live:development:android": "dotenv -e .env.development -- concurrently \"vite --host --mode development\" \"node scripts/live-android.js\"",
    "live:development:ios": "dotenv -e .env.development -- concurrently \"vite --host --mode development\" \"node scripts/live-ios.js\"",
    "live:development": "dotenv -e .env.development -- concurrently \"vite --host --mode development --open\" \"node scripts/live-android.js\" \"node scripts/live-ios.js\"",
    "< Live Reload Staging >": "",
    "live:staging:web": "vite --host --mode staging --open",
    "live:staging:android": "dotenv -e .env.staging -- concurrently \"vite --host --mode staging\" \"node scripts/live-android.js\"",
    "live:staging:ios": "dotenv -e .env.staging -- concurrently \"vite --host --mode staging\" \"node scripts/live-ios.js\"",
    "live:staging": "dotenv -e .env.staging -- concurrently \"vite --host --mode staging --open\" \"node scripts/live-android.js\" \"node scripts/live-ios.js\""
  },
  "devDependencies": {
    "concurrently": "^9.2.4",
    "cross-env": "^10.1.0",
    "dotenv-cli": "^11.0.0",
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/app": "^8.1.1",
    "@capacitor/cli": "^8.4.2",
    "@capacitor/core": "^8.4.2",
    "@capacitor/device": "^8.0.3",
    "remixicon": "^4.9.1"
  }
}
